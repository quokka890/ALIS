[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name="alis"
version = "1"
dependencies=[
    "inquirer",
    "halo"
]

[tool.setuptools.packages]
find = {}